<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Androsova.Models"
             xmlns:style="clr-namespace:Androsova.Models"
             x:Class="Androsova.Views.MainPage"
             BackgroundColor="{x:Static style:PlatformStyle.BackgroundColor}">

    <ContentPage.BindingContext>
        <models:About />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Андросова И. И. Д-Э 317!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Методы и средства проектирования исит"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" 
                BackgroundColor="{x:Static style:PlatformStyle.ButtonColor}"
                TextColor="{x:Static style:PlatformStyle.ButtonTextColor}"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
